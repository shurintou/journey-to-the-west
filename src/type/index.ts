export interface GameResultsList {
    id: number,
    nickname: string,
    cards: number,
    avatar_id: number,
    seat_index: number,
    createdAt: string,
    gameId: number,
    max_combo: number,
}